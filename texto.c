#include <stdio.h>
#include <stdlib.h>

int main(){
    system("clear");
    //variável que guarda texto
    char nome[] = "pedro não";
    printf("Olá %s\n", nome);
    printf("posição %p\n", nome); 

    return 0;
}